# Assets

Put portraits, tokens, maps, UI images, and other campaign visuals here.

Image assets are expected to be versioned through Git LFS:

```bash
git lfs install
git lfs pull
```

Future UI work can read from this folder for tokens, portraits, map art, and encounter visuals.
